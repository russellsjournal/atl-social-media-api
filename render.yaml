services:
  - type: web
    name: atlanta-business-directory
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn backend_api.main:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health
